rm -rf tmp.txt

# 1. 查看输出的响应内容
grep -ni "finish_reason\|choices\|content" vllm_server.log | tail -5 >> tmp.txt

# 2. 查看第二步之后的 compact_gather（看是否持续 enabled=1）
grep -n "compact_gather.*enabled=" vllm_server.log | head -30 >> tmp.txt

# 3. 最关键——查看 FA sparse branch 在 decode 阶段是否终于有了 True
grep -n "FA sparse branch" vllm_server.log | head -50 >> tmp.txt

# 4. 确认输出还是乱码
grep -n "data:.*content" vllm_server.log | tail -10 >> tmp.txt

export VLLM_LOGGING_LEVEL=DEBUG
export VLLM_SPARSE_PROBE_INFO=1
export VLLM_SPARSE_DEBUG_DECODE_TOKENS=1
export VLLM_SPARSE_DEBUG_DECODE_TOKENS_MAX=256
export VLLM_SPARSE_DEBUG_MAX_BLOCKS=256
export VLLM_SPARSE_PERF_STATS=1
export VLLM_SPARSE_PERF_LOG_INTERVAL=10
export VLLM_SPARSE_ASYNC_D2H=0
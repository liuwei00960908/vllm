# 确认同步模式 + num_output_tokens 递增
grep -nE "runtime_mode|_override_sparse_slot_mapping gid=0 req_idx=0" vllm_server.log | head -25 > tmp.txt

# FA sparse branch 路由（关键：是否有 True）
grep -nE "FA sparse branch" vllm_server.log | head -10 > tmp.txt

# 是否还有 mismatch
grep -c "SparseRC:mismatch" vllm_server.log > tmp.txt

# compact gather 成功还是失败
grep -nE "gather_fail_reason|compact_gather.*ENTER|q_head_gather" vllm_server.log | head -20 > tmp.txt

# collect_sparse_features 输出
grep -nE "collect_sparse_features" vllm_server.log | head -10 > tmp.txt

# select 结果
grep -nE "select req_id=" vllm_server.log | head -10 > tmp.txt

# boundary_disable_sparse
grep -c "boundary_disable_sparse" vllm_server.log > tmp.txt

export VLLM_LOGGING_LEVEL=DEBUG
export VLLM_SPARSE_PROBE_INFO=1
export VLLM_SPARSE_DEBUG_DECODE_TOKENS=1
export VLLM_SPARSE_DEBUG_DECODE_TOKENS_MAX=256
export VLLM_SPARSE_DEBUG_MAX_BLOCKS=256
export VLLM_SPARSE_PERF_STATS=1
export VLLM_SPARSE_PERF_LOG_INTERVAL=10
export VLLM_SPARSE_ASYNC_D2H=0
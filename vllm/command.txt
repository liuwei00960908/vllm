rm -rf tmp.txt

# 1. 查看 cascade 相关日志
grep -ni "cascade\|common_prefix" vllm_server.log | head -10 >> tmp.txt

# 2. compact_gather 失败原因（这个很关键）
grep -ni "compact_gather\|enabled=0" vllm_server.log | head -20 >> tmp.txt

# 3. 在 decode 第一步（行 360 附近）前后看完整日志
sed -n '355,400p' vllm_server.log >> tmp.txt

# 4. 在 decode 第二步（行 756 附近）前后看完整日志 >> tmp.txt
sed -n '750,800p' vllm_server.log >> tmp.txt

export VLLM_LOGGING_LEVEL=DEBUG
export VLLM_SPARSE_PROBE_INFO=1
export VLLM_SPARSE_DEBUG_DECODE_TOKENS=1
export VLLM_SPARSE_DEBUG_DECODE_TOKENS_MAX=256
export VLLM_SPARSE_DEBUG_MAX_BLOCKS=256
export VLLM_SPARSE_PERF_STATS=1
export VLLM_SPARSE_PERF_LOG_INTERVAL=10
export VLLM_SPARSE_ASYNC_D2H=0